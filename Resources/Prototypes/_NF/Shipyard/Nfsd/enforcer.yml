# - type: vessel
  # id: Enforcer
  # name: NSF Блюститель
  # description: Небольшой патрульный шаттл быстрого реагирования.
  # price: 21350
  # category: Small
  # group: Security
  # access: Bailiff
  # shuttlePath: /Maps/_NF/Shuttles/Nfsd/enforcer.yml
  # guidebookPage: Null
  # class:
  # - Fighter # Where does this belong?

# - type: gameMap
  # id: Enforcer
  # mapName: 'NSF Блюститель'
  # mapPath: /Maps/_NF/Shuttles/Nfsd/enforcer.yml
  # minPlayers: 0
  # stations:
    # Enforcer:
      # stationProto: StandardFrontierSecurityVessel
      # components:
        # - type: StationNameSetup
          # mapNameTemplate: '{0} Блюститель {1}'
          # nameGenerator:
            # !type:NanotrasenNameGenerator
            # prefixCreator: '14'
        # - type: StationJobs
          # availableJobs:
            # PrisonGuard: [ 0, 0 ]
            # SecurityOfficer: [ 1, 1 ]
